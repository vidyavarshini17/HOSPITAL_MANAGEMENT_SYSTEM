package DBController;

import Model.*;

public class DBHandler {
    //creation of Users object 
    Users user=new Users(null, null, null);
    //creation of Doctors object
    Doctors doctor=new Doctors(0, null, null);
    //creation of Patients object
    Patients patient=new Patients(0, null, 0);
}
